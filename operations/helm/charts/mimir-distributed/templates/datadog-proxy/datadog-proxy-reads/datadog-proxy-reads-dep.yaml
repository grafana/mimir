{{- if .Values.enterprise.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "mimir.resourceName" (dict "ctx" . "component" "datadog-proxy-reads") }}
  labels:
    {{- include "mimir.labels" (dict "ctx" . "component" "datadog-proxy-reads") | nindent 4 }}
  annotations:
    {{- toYaml .Values.datadog.reads.annotations | nindent 4 }}
  namespace: {{ .Release.Namespace | quote }}
spec:
  replicas: {{ .Values.datadog.reads.replicas }}
  selector:
    matchLabels:
      {{- include "mimir.selectorLabels" (dict "ctx" . "component" "datadog-proxy-reads") | nindent 6 }}
  strategy:
    {{- toYaml .Values.datadog.reads.strategy | nindent 4 }}
  template:
    metadata:
      labels:
        {{- include "mimir.podLabels" (dict "ctx" . "component" "datadog-proxy-reads") | nindent 8 }}
      annotations:
        {{- include "mimir.podAnnotations" (dict "ctx" . "component" "datadog-proxy-reads") | nindent 8 }}
      namespace: {{ .Release.Namespace | quote }}
    spec:
      serviceAccountName: {{ template "mimir.serviceAccountName" . }}
      {{- if .Values.datadog.reads.priorityClassName }}
      priorityClassName: {{ .Values.datadog.reads.priorityClassName }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.datadog.reads.securityContext | nindent 8 }}
      initContainers:
        {{- with .Values.datadog.reads.initContainers }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- if .Values.image.pullSecrets }}
      imagePullSecrets:
      {{- range .Values.image.pullSecrets }}
        - name: {{ . }}
      {{- end}}
      {{- end }}
      containers:
        {{- if .Values.datadog.reads.extraContainers }}
        {{ toYaml .Values.datadog.reads.extraContainers | nindent 8 }}
        {{- end }}
        - name: datadog-proxy-reads
          image: "{{ include "mimir.imageReference" . }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - -target=datadog-read-proxy
            - -config.file=/etc/mimir/mimir.yaml
          {{- range $key, $value := .Values.datadog.reads.extraArgs }}
            - "-{{ $key }}={{ $value }}"
          {{- end }}
          volumeMounts:
            {{- if .Values.datadog.reads.extraVolumeMounts }}
              {{ toYaml .Values.datadog.reads.extraVolumeMounts | nindent 12}}
            {{- end }}
            - name: config
              mountPath: /etc/mimir
            - name: runtime-config
              mountPath: /var/{{ include "mimir.name" . }}
            - name: license
              mountPath: /license
            - name: storage
              mountPath: "/data"
              subPath: {{ .Values.datadog.reads.persistence.subPath }}
            - name: active-queries
              mountPath: /active-query-tracker
          ports:
            - name: http-metrics
              containerPort: {{ include "mimir.serverHttpListenPort" . }}
              protocol: TCP
            - name: grpc
              containerPort: {{ include "mimir.serverGrpcListenPort" . }}
              protocol: TCP
          livenessProbe:
            {{- toYaml .Values.datadog.reads.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.datadog.reads.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.datadog.reads.resources | nindent 12 }}
          securityContext:
            {{- toYaml .Values.datadog.reads.containerSecurityContext | nindent 12 }}
          env:
            {{- with .Values.global.extraEnv }}
              {{ toYaml . | nindent 12 }}
            {{- end }}
            {{- with .Values.datadog.reads.env }}
              {{ toYaml . | nindent 12 }}
            {{- end }}
          envFrom:
            {{- with .Values.global.extraEnvFrom }}
              {{- toYaml . | nindent 12 }}
            {{- end }}
            {{- with .Values.datadog.reads.extraEnvFrom }}
              {{- toYaml . | nindent 12 }}
            {{- end }}
      nodeSelector:
        {{- toYaml .Values.datadog.reads.nodeSelector | nindent 8 }}
      affinity:
        {{- toYaml .Values.datadog.reads.affinity | nindent 8 }}
      tolerations:
        {{- toYaml .Values.datadog.reads.tolerations | nindent 8 }}
      terminationGracePeriodSeconds: {{ .Values.datadog.reads.terminationGracePeriodSeconds }}
      volumes:
        - name: config
          {{- include "mimir.configVolume" . | nindent 10 }}
        - name: runtime-config
          configMap:
            name: {{ template "mimir.fullname" . }}-runtime
        {{- if .Values.datadog.reads.extraVolumes }}
        {{ toYaml .Values.datadog.reads.extraVolumes | indent 8}}
        {{- end }}
        - name: license
          secret:
            secretName: {{ tpl .Values.license.secretName . }}
        - name: storage
          emptyDir: {}
        - name: active-queries
          emptyDir: {}
{{- end -}}
