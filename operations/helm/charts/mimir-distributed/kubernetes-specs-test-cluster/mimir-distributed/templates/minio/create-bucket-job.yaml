---
# Source: mimir-distributed/templates/minio/create-bucket-job.yaml
# Minio provides post-install hook to create bucket
# however the hook won't be executed if helm install is run
# with --wait flag. Hence this job is a workaround for that.
# See https://github.com/grafana/mimir/issues/2464
apiVersion: batch/v1
kind: Job
metadata:
  name: databricks-gem-mimir-make-minio-buckets
  namespace: "mimir"
  labels:
    app: mimir-distributed-make-bucket-job
    chart: mimir-distributed-4.3.1
    release: databricks-gem
    heritage: Helm
spec:
  template:
    metadata:
      labels:
        app: mimir-distributed-job
        release: databricks-gem
    spec:
      restartPolicy: OnFailure      
      volumes:
        - name: minio-configuration
          projected:
            sources:
            - configMap:
                name: databricks-gem-minio
            - secret:
                name: databricks-gem-minio
      containers:
      - name: minio-mc
        image: "quay.io/minio/mc:RELEASE.2022-12-13T00-23-28Z"
        imagePullPolicy: IfNotPresent
        command: ["/bin/sh", "/config/initialize"]
        env:
          - name: MINIO_ENDPOINT
            value: minio-svc.thanos
          - name: MINIO_PORT
            value: "9000"
        volumeMounts:
          - name: minio-configuration
            mountPath: /config
        resources:
          requests:
            memory: 128Mi
