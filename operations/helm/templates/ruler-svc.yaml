apiVersion: v1
kind: Service
metadata:
  name: {{ template "mimir.fullname" . }}-ruler
  labels:
    app: {{ template "mimir.name" . }}-ruler
    chart: {{ template "mimir.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    {{- with .Values.ruler.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  annotations:
    {{- toYaml .Values.ruler.annotations | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.config.server.http_listen_port }}
      protocol: TCP
      name: http-metrics
      targetPort: http-metrics
  selector:
    app: {{ template "mimir.name" . }}-ruler
    release: {{ .Release.Name }}
