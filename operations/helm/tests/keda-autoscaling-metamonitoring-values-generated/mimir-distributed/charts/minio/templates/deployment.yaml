---
# Source: mimir-distributed/charts/minio/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: keda-autoscaling-metamonitoring-values-minio
  labels:
    app: minio
    chart: minio-5.1.0
    release: keda-autoscaling-metamonitoring-values
    heritage: Helm
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 100%
      maxUnavailable: 0
  replicas: 1
  selector:
    matchLabels:
      app: minio
      release: keda-autoscaling-metamonitoring-values
  template:
    metadata:
      name: keda-autoscaling-metamonitoring-values-minio
      labels:
        app: minio
        release: keda-autoscaling-metamonitoring-values
      annotations:
        checksum/secrets: 8ccf3d52d7b9dcf6ac8d1a4374a9687e08c998f82ad4e236a2c51e6bbb4c1d16
        checksum/config: 1562b927ebf7c2ea96def81158fdfa048b7ab6a10e84b0029989a5fdd0249b2a
    spec:
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
      
      serviceAccountName: minio-sa
      containers:
        - name: minio
          image: "quay.io/minio/minio:RELEASE.2024-03-03T17-50-39Z"
          imagePullPolicy: IfNotPresent
          command:
            - "/bin/sh"
            - "-ce"
            - "/usr/bin/docker-entrypoint.sh minio server /export -S /etc/minio/certs/ --address :9000 --console-address :9001"
          volumeMounts:
            - name: minio-user
              mountPath: "/tmp/credentials"
              readOnly: true
            - name: export
              mountPath: /export            
          ports:
            - name: http
              containerPort: 9000
            - name: http-console
              containerPort: 9001
          env:
            - name: MINIO_ROOT_USER
              valueFrom:
                secretKeyRef:
                  name: keda-autoscaling-metamonitoring-values-minio
                  key: rootUser
            - name: MINIO_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: keda-autoscaling-metamonitoring-values-minio
                  key: rootPassword
            - name: MINIO_PROMETHEUS_AUTH_TYPE
              value: "public"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi      
      volumes:
        - name: export
          persistentVolumeClaim:
            claimName: keda-autoscaling-metamonitoring-values-minio
        - name: minio-user
          secret:
            secretName: keda-autoscaling-metamonitoring-values-minio
