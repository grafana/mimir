---
# Source: mimir-distributed/templates/datadog-proxy/datadog-proxy-reads/datadog-proxy-reads-svc.yaml
apiVersion: v1
kind: Service
metadata:
  name: test-enterprise-legacy-label-values-enterprise-metrics-datadog-proxy-reads
  labels:
    app: enterprise-metrics-datadog-proxy-reads
    heritage: Helm
    release: test-enterprise-legacy-label-values
  annotations:
    {}
  namespace: "citestns"
spec:
  type: ClusterIP
  ports:
    - port: 8080
      protocol: TCP
      name: http-metrics
      targetPort: http-metrics
    - port: 9095
      protocol: TCP
      name: grpc
      targetPort: grpc
  selector:
    app: enterprise-metrics-datadog-proxy-reads
    release: test-enterprise-legacy-label-values
