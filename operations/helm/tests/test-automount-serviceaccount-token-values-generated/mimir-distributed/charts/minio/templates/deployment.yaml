---
# Source: mimir-distributed/charts/minio/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-automount-serviceaccount-token-values-minio
  labels:
    app: minio
    chart: minio-5.4.0
    release: test-automount-serviceaccount-token-values
    heritage: Helm
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 100%
      maxUnavailable: 0
  replicas: 1
  selector:
    matchLabels:
      app: minio
      release: test-automount-serviceaccount-token-values
  template:
    metadata:
      name: test-automount-serviceaccount-token-values-minio
      labels:
        app: minio
        release: test-automount-serviceaccount-token-values
      annotations:
        checksum/secrets: 9a800010c5216aaa1262ce71d269fb6b110448f6fe071591eac3560bcec4055b
        checksum/config: 4e23d943eb1d8d88131c7aa94165769f8bb44c3fb0d66e54ec4b3412fdb8ff9c
    spec:
      securityContext:
        
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        runAsGroup: 1000
        runAsUser: 1000
      
      serviceAccountName: minio-sa
      containers:
        - name: minio
          image: "quay.io/minio/minio:RELEASE.2024-12-18T13-15-44Z"
          imagePullPolicy: IfNotPresent
          command:
            - "/bin/sh"
            - "-ce"
            - "/usr/bin/docker-entrypoint.sh minio server /export -S /etc/minio/certs/ --address :9000 --console-address :9001"
          volumeMounts:
            - name: minio-user
              mountPath: "/tmp/credentials"
              readOnly: true
            - name: export
              mountPath: /export            
          ports:
            - name: http
              containerPort: 9000
            - name: http-console
              containerPort: 9001
          env:
            - name: MINIO_ROOT_USER
              valueFrom:
                secretKeyRef:
                  name: test-automount-serviceaccount-token-values-minio
                  key: rootUser
            - name: MINIO_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: test-automount-serviceaccount-token-values-minio
                  key: rootPassword
            - name: MINIO_PROMETHEUS_AUTH_TYPE
              value: "public"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
          securityContext: 
            readOnlyRootFilesystem: false      
      volumes:
        - name: export
          persistentVolumeClaim:
            claimName: test-automount-serviceaccount-token-values-minio
        - name: minio-user
          secret:
            secretName: test-automount-serviceaccount-token-values-minio        

