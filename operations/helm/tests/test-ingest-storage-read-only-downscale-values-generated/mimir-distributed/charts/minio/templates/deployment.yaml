---
# Source: mimir-distributed/charts/minio/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-ingest-storage-read-only-downscale-values-minio
  labels:
    app: minio
    chart: minio-5.4.0
    release: test-ingest-storage-read-only-downscale-values
    heritage: Helm
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 100%
      maxUnavailable: 0
  replicas: 1
  selector:
    matchLabels:
      app: minio
      release: test-ingest-storage-read-only-downscale-values
  template:
    metadata:
      name: test-ingest-storage-read-only-downscale-values-minio
      labels:
        app: minio
        release: test-ingest-storage-read-only-downscale-values
      annotations:
        checksum/secrets: 2e7f0590f28277eec5d16bc712a095fed88a92e2734268804ac969e414a25650
        checksum/config: f450dfe47ba3fa0e77b3e6d5c40c392bd7064ff7154d93abf47d96640dc350bc
    spec:
      securityContext:
        
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        runAsGroup: 1000
        runAsUser: 1000
      
      serviceAccountName: minio-sa
      containers:
        - name: minio
          image: "quay.io/minio/minio:RELEASE.2024-12-18T13-15-44Z"
          imagePullPolicy: IfNotPresent
          command:
            - "/bin/sh"
            - "-ce"
            - "/usr/bin/docker-entrypoint.sh minio server /export -S /etc/minio/certs/ --address :9000 --console-address :9001"
          volumeMounts:
            - name: minio-user
              mountPath: "/tmp/credentials"
              readOnly: true
            - name: export
              mountPath: /export            
          ports:
            - name: http
              containerPort: 9000
            - name: http-console
              containerPort: 9001
          env:
            - name: MINIO_ROOT_USER
              valueFrom:
                secretKeyRef:
                  name: test-ingest-storage-read-only-downscale-values-minio
                  key: rootUser
            - name: MINIO_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: test-ingest-storage-read-only-downscale-values-minio
                  key: rootPassword
            - name: MINIO_PROMETHEUS_AUTH_TYPE
              value: "public"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
          securityContext: 
            readOnlyRootFilesystem: false      
      volumes:
        - name: export
          persistentVolumeClaim:
            claimName: test-ingest-storage-read-only-downscale-values-minio
        - name: minio-user
          secret:
            secretName: test-ingest-storage-read-only-downscale-values-minio        

