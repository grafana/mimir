kind: MimirConfig
apiVersion: grafana.com/v1alpha1
metadata:
  name: not-important
config:
  activity_tracker:
    filepath:
  alertmanager:
    data_dir:
    external_url:
    sharding_ring:
      kvstore:
        consul:
          host:
        store:
  blocks_storage:
    backend:
    gcs:
      bucket_name:
    bucket_store:
      sync_dir:
      metadata_cache:
        memcached:
          addresses:
      index_cache:
        memcached:
          addresses:
      chunks_cache:
        memcached:
          addresses:
    tsdb:
      dir:
  ingester:
    ring:
      tokens_file_path:
      kvstore:
        prefix:
  ruler:
    rule_path:
    alertmanager_url:
  runtime_config:
    file:
  store_gateway:
    sharding_ring:
      tokens_file_path:
      kvstore:
        prefix:
  frontend:
    results_cache:
      memcached:
        addresses:
  distributor:
    ha_tracker:
      kvstore:
        etcd:
          endpoints:
        prefix:
    ring:
      kvstore:
        prefix:
  frontend_worker:
    frontend_address:
  memberlist:
    join_members:
  alertmanager_storage:
    backend:
    gcs:
      bucket_name:
  ruler_storage:
    backend:
    gcs:
      bucket_name:
  compactor:
    data_dir:
    sharding_ring:
      kvstore:
        prefix:
