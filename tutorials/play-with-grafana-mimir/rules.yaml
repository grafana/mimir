# Example rules file to load to Grafana Mimir using the ruler API.
groups:
  - name: demo
    rules:
      - record: count:up
        expr: count(up)
      - alert: AlertAlwaysFiring
        expr: 1
        for: 0s
        annotations:
          summary: This is an always firing test alert.
