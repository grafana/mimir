name: Vendor hotfix workflow
on:
  pull_request:
    # types:
    #   - closed
    branches:
      - main
      - ^r\d{3}$
    

jobs:
  hotfix:
    # if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
    - run: |
      echo "Hello world"
      