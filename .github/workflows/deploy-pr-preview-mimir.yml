name: Deploy pr preview (Mimir)

on:
  pull_request:
    types:
      - opened
      - synchronize
      - closed
    paths:
      - "docs/sources/mimir/**"

jobs:
  deploy-pr-preview:
    permissions:
      contents: read # Clone repository.
      id-token: write # Fetch Vault secrets.
      pull-requests: write # Create or update pull request comments.
      statuses: write # Update GitHub status check with deploy preview link.
    if: "!github.event.pull_request.head.repo.fork"
    uses: grafana/writers-toolkit/.github/workflows/deploy-preview.yml@4dbecae7443c69b3e5fdfeeac7037a339cb82e4c # main
    with:
      branch: ${{ github.head_ref }}
      event_number: ${{ github.event.number }}
      repo: mimir
      sha: ${{ github.event.pull_request.head.sha }}
      sources: |
        [
          {
            "index_file": null,
            "relative_prefix": "/docs/mimir/next/",
            "repo": "mimir",
            "source_directory": "docs/sources/mimir",
            "website_directory": "content/docs/mimir/next"
          }
        ]
      title: Mimir
