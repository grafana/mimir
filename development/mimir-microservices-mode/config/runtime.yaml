# This file can be used to set overrides or other runtime config.

# Runtime configuration for "multi" KV.
# multi_kv_config:
#  mirror_enabled: false
#  primary: consul
overrides:
    "anonymous":
      limited_queries:
        - pattern: "activity_tracker_failed_total"
          frequency: 1m
