---
phase: 02-duplicate-utc-day-detection-in-backfill-verifier
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - pkg/mimirtool/commands/backfill.go
  - pkg/mimirtool/client/backfill_test.go
  - CHANGELOG.md
autonomous: true
requirements: [CHECK-04]
tags: [mimirtool, backfill, verification, duplicate-day, cli, changelog]
must_haves:
  truths:
    - "The CLI's `mimirtool backfill` command registers the DuplicateDayVerifier via verify.WithBatchCheck during verifier construction."
    - "Running `mimirtool backfill` against a batch containing two blocks whose half-open [MinTime, MaxTime) ranges land on the same UTC day exits non-zero and uploads ZERO blocks."
    - "Running `mimirtool backfill` against a batch where every block covers a distinct UTC day verifies and uploads successfully (no regression on the existing Phase 1 happy-path tests)."
    - "An end-to-end test in pkg/mimirtool/client/backfill_test.go exercises the duplicate-day path with a real two-block batch and an httptest server, asserting zero upload requests."
    - "The batch check runs when per-block fail-fast has not tripped; the end-to-end test uses two blocks that pass per-block checks so the batch stage runs under the default fail-fast mode."
    - "CHANGELOG.md gains a new [FEATURE] entry for CHECK-04 with the next PR number from ./tools/github-next-pr-number.sh, placed above the Phase 1 entry (#15115) in the [main / unreleased] section."
    - "`make format` leaves no changes beyond the intended edits; lint passes on all touched packages; `go test ./pkg/mimirtool/... -count=1` passes."
    - "No changes to Verifier.Run, the BlockVerifier interface, the BatchVerifier interface, or any other Phase 1 public API."
  artifacts:
    - path: pkg/mimirtool/commands/backfill.go
      provides: "Modified to call verify.WithBatchCheck(verify.NewDuplicateDayVerifier(logger)) in the NewVerifier assembly"
    - path: pkg/mimirtool/client/backfill_test.go
      provides: "New test TestBackfillWithOptions_DuplicateDayRejected asserting zero uploads when two blocks share a UTC day"
    - path: CHANGELOG.md
      provides: "New [FEATURE] entry for CHECK-04 at the top of the unreleased section"
  key_links:
    - from: pkg/mimirtool/commands/backfill.go
      to: pkg/mimirtool/backfill/verify/duplicateday.go
      via: "verify.WithBatchCheck(verify.NewDuplicateDayVerifier(logger)) added after the two existing WithBlockCheck lines"
      pattern: "WithBatchCheck.*NewDuplicateDayVerifier"
    - from: pkg/mimirtool/client/backfill_test.go
      to: pkg/mimirtool/backfill/verify
      via: "constructs a verify.Verifier registered with WithBlockCheck(NewWellFormedVerifier), WithBlockCheck(NewSingleUTCDayVerifier), and WithBatchCheck(NewDuplicateDayVerifier)"
      pattern: "WithBatchCheck.*NewDuplicateDayVerifier"
---

<objective>
Wire Phase 2's `DuplicateDayVerifier` into the shipping CLI, prove the end-to-end behaviour with an httptest-based zero-upload test, and land the CHANGELOG entry. This plan turns the Phase 2 capability into a user-facing behaviour change.

Purpose: Closes CHECK-04 end-to-end. Without this plan, the verifier exists but never runs against real user batches.

Output: Three file edits (no new files), a new test function, a CHANGELOG [FEATURE] entry, and green `make format` / lint / test gates.

### Locked design (from planning context)

- **CLI wiring:** exactly one new line inside the existing `verify.NewVerifier(...)` call in `pkg/mimirtool/commands/backfill.go`. Placement: AFTER the two existing `WithBlockCheck` lines. No new flag. The check is always on (matches Phase 1's "no escape hatch" principle).
- **Batch stage runs only when per-block fail-fast hasn't tripped.** Per verify.go:169, if `opts.failFast == true` AND any per-block check fails, the batch stage is skipped. Users in the default CLI mode (fail-fast + deep) with ONE bad block (e.g. cross-midnight) will see that block's failure but not the duplicate-day check. This is the intended Phase 1 semantics; Phase 2 does not change it. Document this in the CHANGELOG entry so users know `-full-report` exercises duplicate-day detection alongside per-block failures.
- **End-to-end test strategy:** two blocks on the same UTC day, both well-formed and both single-day (so they pass per-block checks), so the batch stage WILL run even in fail-fast mode. That keeps the test simple and exercises the real production path.
- **CHANGELOG entry format:** new `[FEATURE]` line near the top of the unreleased/main section, PR number pulled at implementation time from `./tools/github-next-pr-number.sh`. Do not modify the existing Phase 1 entry (#15115) — it has already shipped its own PR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-duplicate-utc-day-detection-in-backfill-verifier/02-01-PLAN.md
@CLAUDE.md
@pkg/CLAUDE.md
@pkg/mimirtool/backfill/verify/verify.go
@pkg/mimirtool/backfill/verify/options.go
@pkg/mimirtool/backfill/verify/duplicateday.go
@pkg/mimirtool/commands/backfill.go
@pkg/mimirtool/client/backfill.go
@pkg/mimirtool/client/backfill_test.go

<interfaces>
From Plan 02-01 (just shipped):
```
// pkg/mimirtool/backfill/verify/duplicateday.go
func NewDuplicateDayVerifier(logger log.Logger) *DuplicateDayVerifier
func (v *DuplicateDayVerifier) Name() string  // returns "duplicate-day"
func (v *DuplicateDayVerifier) Verify(ctx context.Context, blocks []BlockRef) error
```

From pkg/mimirtool/backfill/verify/options.go (Phase 1):
```
func WithBatchCheck(v BatchVerifier) Option
func WithFailFast(b bool) Option
func WithMode(m Mode) Option
```

Batch-stage skip rule at verify.go:169:
```
batchShouldRun := !report.HasFailures() || !v.opts.failFast
```
Implication: if fail-fast is TRUE and any per-block check fails, batch checks do NOT run. Tests exercising the duplicate-day path must either (a) use blocks that pass per-block checks, or (b) set WithFailFast(false). Plan uses option (a).

Current state of pkg/mimirtool/commands/backfill.go around lines 158-164:
```
verifier := verify.NewVerifier(logger,
    verify.WithMode(mode),
    verify.WithFailFast(!c.fullReport),
    verify.WithConcurrency(c.verifyConcurrency),
    verify.WithBlockCheck(verify.NewWellFormedVerifier(logger, mode)),
    verify.WithBlockCheck(verify.NewSingleUTCDayVerifier(logger)),
)
```

Current state of pkg/mimirtool/client/backfill_test.go helpers (re-use, do NOT recreate):
- `uploadCountingServer(t)` returns an httptest.Server + upload counter.
- `newTestClient(t, ts)` returns a *MimirClient.
- `generateValidBlockInline(t, parent, samples)` returns (dir, *block.Meta) for a 3-series real TSDB block built via `block.GenerateBlockFromSpec`.
- `sampleAt(ts, v)` returns a `chunks.Sample` via `testutil.Sample{TS, Val}`.
- `msPerDay` constant already declared in this test file at line 26.

Existing tests that MUST still pass after the wiring change (no regressions):
- TestBackfillWithOptions_VerificationFailureZeroUploads
- TestBackfillWithOptions_DryRunZeroUploads
- TestBackfillWithOptions_ValidBatchUploads
- TestBackfill_LegacyWrapperBehavesAsBefore

None of the existing tests register a DuplicateDayVerifier, so they are unaffected by Task 1's CLI-level wiring. They call `BackfillWithOptions` with their own verifier.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire DuplicateDayVerifier into the CLI</name>
  <files>pkg/mimirtool/commands/backfill.go</files>
  <read_first>
    - pkg/mimirtool/commands/backfill.go (the current verify.NewVerifier call at lines 158-164)
    - pkg/mimirtool/backfill/verify/duplicateday.go (to confirm the symbol name)
    - pkg/mimirtool/backfill/verify/options.go (WithBatchCheck signature)
  </read_first>
  <action>
In `pkg/mimirtool/commands/backfill.go`, locate the `verify.NewVerifier(...)` call inside the `backfill` method (currently lines 158-164). The current state is:

```
verifier := verify.NewVerifier(logger,
    verify.WithMode(mode),
    verify.WithFailFast(!c.fullReport),
    verify.WithConcurrency(c.verifyConcurrency),
    verify.WithBlockCheck(verify.NewWellFormedVerifier(logger, mode)),
    verify.WithBlockCheck(verify.NewSingleUTCDayVerifier(logger)),
)
```

Add exactly ONE new line AFTER the two `WithBlockCheck` lines:

```
verifier := verify.NewVerifier(logger,
    verify.WithMode(mode),
    verify.WithFailFast(!c.fullReport),
    verify.WithConcurrency(c.verifyConcurrency),
    verify.WithBlockCheck(verify.NewWellFormedVerifier(logger, mode)),
    verify.WithBlockCheck(verify.NewSingleUTCDayVerifier(logger)),
    verify.WithBatchCheck(verify.NewDuplicateDayVerifier(logger)),
)
```

Do NOT:
- Add a new CLI flag (check is always on per Phase 1's no-escape-hatch principle).
- Reorder the existing options.
- Change the logger argument passed to `NewDuplicateDayVerifier` (use the same `logger` variable as the other two constructors).
- Touch imports — `verify` is already imported.
- Modify any other file in this task.

Run `go build ./pkg/mimirtool/commands/...` and `go vet ./pkg/mimirtool/commands/...` after the edit.
  </action>
  <verify>
    <automated>go build ./pkg/mimirtool/commands/... && go vet ./pkg/mimirtool/commands/... && grep -q 'verify.WithBatchCheck(verify.NewDuplicateDayVerifier(logger))' pkg/mimirtool/commands/backfill.go</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'verify.WithBatchCheck(verify.NewDuplicateDayVerifier(logger))' pkg/mimirtool/commands/backfill.go` returns exactly 1
    - The WithBatchCheck line appears AFTER both existing WithBlockCheck lines in the file (verify by `grep -n` line numbers)
    - `go build ./pkg/mimirtool/commands/...` exits 0
    - `go vet ./pkg/mimirtool/commands/...` exits 0
    - `git diff pkg/mimirtool/commands/backfill.go` shows exactly one net added line, no removed lines (a +1/-0 diff)
    - No new import lines added to this file (`git diff` should not show any `+\t"..."` lines inside an import block)
  </acceptance_criteria>
  <done>One-line wiring change lands in commands/backfill.go; build and vet clean; no collateral edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: End-to-end test — duplicate UTC day rejected, zero uploads</name>
  <files>pkg/mimirtool/client/backfill_test.go</files>
  <read_first>
    - pkg/mimirtool/client/backfill_test.go (existing helpers: uploadCountingServer, newTestClient, generateValidBlockInline, sampleAt)
    - pkg/mimirtool/backfill/verify/duplicateday.go (NewDuplicateDayVerifier signature)
    - pkg/mimirtool/backfill/verify/verify.go (batch-stage skip rule at lines 169-182)
  </read_first>
  <behavior>
    New test `TestBackfillWithOptions_DuplicateDayRejected`:
    - Two well-formed 3-series blocks, both confined to UTC day 0. Both pass WellFormed + SingleUTCDay checks so fail-fast does not trip before the batch stage.
    - Constructs a `verify.Verifier` with: WithMode(Medium), WithBlockCheck(NewWellFormedVerifier, Medium), WithBlockCheck(NewSingleUTCDayVerifier), WithBatchCheck(NewDuplicateDayVerifier).
    - Calls `cli.BackfillWithOptions(ctx, [dirA, dirB], 1ms, v, false)` (dryRun=false).
    - Asserts non-nil error, error message contains "verification failed" (the standard Report.Err() summary format), and upload counter is exactly 0.
  </behavior>
  <action>
Append a new test function to `pkg/mimirtool/client/backfill_test.go`. Do NOT modify any existing helper or test.

Exact test function to add at the end of the file:

```
// TestBackfillWithOptions_DuplicateDayRejected verifies CHECK-04: a batch
// containing two blocks whose time ranges land on the same UTC day is
// rejected by the BatchVerifier, and zero upload requests reach the server.
//
// Both blocks are well-formed and individually single-UTC-day, so per-block
// checks pass and fail-fast does not trip before the batch stage runs. This
// keeps the test exercising the real production code path used by mimirtool
// backfill (medium mode here skips the CRC walk — that path is covered in
// Phase 1 tests).
func TestBackfillWithOptions_DuplicateDayRejected(t *testing.T) {
	ts, uploadCount := uploadCountingServer(t)
	cli := newTestClient(t, ts)

	// Two blocks, both confined to UTC day 0 ([0, msPerDay)).
	dirA, _ := generateValidBlockInline(t, t.TempDir(), []chunks.Sample{
		sampleAt(1_000, 1.0), sampleAt(2_000, 2.0),
	})
	dirB, _ := generateValidBlockInline(t, t.TempDir(), []chunks.Sample{
		sampleAt(3_000, 3.0), sampleAt(4_000, 4.0),
	})

	v := verify.NewVerifier(log.NewNopLogger(),
		verify.WithMode(verify.Medium),
		verify.WithBlockCheck(verify.NewWellFormedVerifier(log.NewNopLogger(), verify.Medium)),
		verify.WithBlockCheck(verify.NewSingleUTCDayVerifier(log.NewNopLogger())),
		verify.WithBatchCheck(verify.NewDuplicateDayVerifier(log.NewNopLogger())),
	)

	err := cli.BackfillWithOptions(context.Background(),
		[]string{dirA, dirB}, time.Millisecond, v, false)
	require.Error(t, err, "batch with two same-UTC-day blocks must fail verification")
	assert.Contains(t, err.Error(), "verification failed",
		"aggregated error is the Report summary; per-check detail is in log lines")
	assert.Equal(t, int64(0), uploadCount.Load(),
		"no /api/v1/upload/block/... request should have been issued when duplicate-day is detected")
}
```

Notes:
- All imports already exist in this file: `verify`, `log`, `assert`, `require`, `chunks`, `time`, `context`, `testing`. Run `goimports` if in doubt.
- Do NOT call `verify.WithFailFast(false)`. The default fail-fast=true is fine because both blocks pass per-block checks, so fail-fast never trips and the batch stage runs. This matches the default CLI mode, making the test representative.
- The `msPerDay` const is already declared in this test file (line 26); this test does not need it because both blocks stay in day 0.

After adding the test:
1. Run `go build ./pkg/mimirtool/client/...` — must exit 0.
2. Run `go test ./pkg/mimirtool/client/... -run TestBackfillWithOptions_DuplicateDayRejected -count=1` — must pass.
3. Run `go test ./pkg/mimirtool/client/... -count=1` — all existing tests must still pass.

Do NOT: change existing helpers, add new helpers (existing ones suffice), touch production code in `client/backfill.go`, mock the verify package.
  </action>
  <verify>
    <automated>go test ./pkg/mimirtool/client/... -run TestBackfillWithOptions_DuplicateDayRejected -count=1 && go test ./pkg/mimirtool/client/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'func TestBackfillWithOptions_DuplicateDayRejected' pkg/mimirtool/client/backfill_test.go` succeeds
    - `grep -q 'verify.WithBatchCheck(verify.NewDuplicateDayVerifier' pkg/mimirtool/client/backfill_test.go` succeeds
    - `go test ./pkg/mimirtool/client/... -run TestBackfillWithOptions_DuplicateDayRejected -count=1` exits 0
    - `go test ./pkg/mimirtool/client/... -count=1` exits 0 (all existing tests still pass)
    - `go vet ./pkg/mimirtool/client/...` exits 0
    - No new imports added to backfill_test.go (`git diff` shows no `+\t"..."` line inside the import block)
  </acceptance_criteria>
  <done>New test passes; no pre-existing test regresses; no import churn.</done>
</task>

<task type="auto">
  <name>Task 3: CHANGELOG, make format, lint, full test sweep</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (top 50 lines — confirm the current unreleased section and the Phase 1 entry format at line 40)
    - ./tools/github-next-pr-number.sh (to get the next PR number at execution time)
  </read_first>
  <action>
**Step 1 — Grab the next PR number:**
```
PR_NUM=$(bash ./tools/github-next-pr-number.sh)
echo "Next PR number: $PR_NUM"
```

**Step 2 — Add CHANGELOG entry.** Insert a new line in `CHANGELOG.md` immediately ABOVE the existing Phase 1 entry (currently line 40, the one ending in `#15115`). The Phase 1 entry is a `[FEATURE]` in the mimirtool block; putting the new entry directly above it keeps mimirtool entries grouped and preserves the `[CHANGE]` / `[FEATURE]` / `[ENHANCEMENT]` / `[BUGFIX]` ordering convention.

Exact text to insert (substitute the real PR number for `<PR_NUM>`):

```
* [FEATURE] Mimirtool: `backfill` command now rejects a batch when two or more blocks cover the same UTC day. The check runs after per-block verification; in the default fail-fast mode, per-block failures short-circuit the run before the duplicate-day check (use `-full-report` to surface duplicate-day findings alongside per-block failures). #<PR_NUM>
```

Do NOT modify the existing Phase 1 entry (#15115) — it has already shipped in its own PR and this is a follow-up.

**Step 3 — Run `make format`:**
```
make format
```
If any files beyond the three in this plan get reformatted, inspect the diff and confirm the changes are formatting-only (e.g. goimports reordering import groups). Stage those changes too.

**Step 4 — Run the lint sweep:**
```
make BUILD_IN_CONTAINER=false golangci-lint 2>&1 | tee /tmp/phase2-lint.log
```
If that target is unavailable in your environment, fall back to:
```
golangci-lint run ./pkg/mimirtool/backfill/verify/... ./pkg/mimirtool/client/... ./pkg/mimirtool/commands/...
```
Must exit 0. Fix real issues; do NOT add `//nolint` without explicit inline justification.

**Step 5 — Full package test:**
```
go test ./pkg/mimirtool/... -count=1
```
Must exit 0. Final green-light check.

**Step 6 — `make doc` sanity:** Phase 2 adds no new CLI flags and no config options, so `make reference-help` / `make doc` are NOT required. Confirm by inspecting `git diff pkg/mimirtool/commands/backfill.go` — if the diff is only the single `WithBatchCheck` line and no flag registration, skip `make doc`.

Do NOT: modify CHANGELOG entries for unrelated PRs, reformat unrelated files, introduce `//nolint` without justification, commit anything (this plan is executed; committing happens when Owen decides to open the PR).
  </action>
  <verify>
    <automated>grep -qE 'rejects a batch when two or more blocks cover the same UTC day.*#[0-9]+' CHANGELOG.md && make format && go test ./pkg/mimirtool/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'rejects a batch when two or more blocks cover the same UTC day' CHANGELOG.md` returns exactly 1
    - The new CHANGELOG line matches the regex `^\* \[FEATURE\] Mimirtool: .*duplicate-day.*#[0-9]+$` OR the looser regex `rejects a batch when two or more blocks cover the same UTC day.*#[0-9]+`
    - The new CHANGELOG line's line number is LESS THAN the line number of the existing `#15115` entry (so the new entry appears above it): verify via `awk '/rejects a batch when two or more blocks/ {print NR}' CHANGELOG.md` returning a number less than `awk '/#15115/ {print NR}' CHANGELOG.md`
    - `make format` leaves `git diff` showing ONLY the three files in `files_modified` (plus any goimports-legitimate reordering in those same files)
    - `go test ./pkg/mimirtool/... -count=1` exits 0
    - Lint sweep (`make BUILD_IN_CONTAINER=false golangci-lint` or `golangci-lint run ./pkg/mimirtool/...`) exits 0
    - `git diff pkg/mimirtool/commands/backfill.go` shows no new CLI-flag registration (confirming `make doc` is genuinely a no-op)
  </acceptance_criteria>
  <done>CHANGELOG entry present with a real PR number; format/lint/test gates all green; PR-ready state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mimirtool CLI -> verify package | User-supplied block directories flow through `ReadMetaFromDir` before reaching the verifier; meta fields are structurally validated upstream. |
| mimirtool -> Mimir server | Network boundary. Rejecting duplicate-day batches client-side is strictly a pre-flight step; the server still validates on upload. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | I (Info disclosure) | CHANGELOG error message guidance | accept | The entry documents how users see duplicate-day errors; contains no secrets and only operator-facing identifiers. |
| T-02-06 | T (Tampering) | CLI wiring (single added line) | mitigate | The addition is a pure append to an existing functional-options call; it cannot silently replace any Phase 1 check. Acceptance criterion `+1/-0 diff` enforces this mechanically. |
| T-02-07 | D (DoS) | Test fixtures generate real TSDB blocks on disk | accept | `t.TempDir` cleans up automatically; blocks are tiny (2 samples each). Upper bound on disk usage is the Go test harness's normal budget. |
| T-02-08 | R (Repudiation) | CHANGELOG PR-number fabrication | mitigate | Acceptance criterion requires `#[0-9]+` in the entry; executor MUST run `./tools/github-next-pr-number.sh` (not invent a number). |
</threat_model>

<verification>
Whole-project verification after all three tasks:

```
go build ./pkg/mimirtool/...
go vet ./pkg/mimirtool/...
go test ./pkg/mimirtool/... -count=1
make format   # idempotent
golangci-lint run ./pkg/mimirtool/backfill/verify/... ./pkg/mimirtool/client/... ./pkg/mimirtool/commands/...
```

All must exit 0. `make format` must report no further changes on a second run.

Additionally, a spot check on the full run:
- `git diff --stat` should show exactly these files modified since Plan 02-01 finished: `pkg/mimirtool/commands/backfill.go`, `pkg/mimirtool/client/backfill_test.go`, `CHANGELOG.md`.
- `git diff pkg/mimirtool/commands/backfill.go` should be +1/-0.
- `git diff CHANGELOG.md` should add exactly one line.
</verification>

<success_criteria>
- CLI registers the DuplicateDayVerifier: `grep -q 'verify.WithBatchCheck(verify.NewDuplicateDayVerifier(logger))' pkg/mimirtool/commands/backfill.go`
- End-to-end test passes: `go test ./pkg/mimirtool/client/... -run TestBackfillWithOptions_DuplicateDayRejected -count=1` exits 0
- No pre-existing test regresses: `go test ./pkg/mimirtool/... -count=1` exits 0
- CHANGELOG entry present with a real PR number matching `#[0-9]+`
- `make format` leaves no further changes
- Lint passes on all touched packages
- Phase 2 Success Criteria #1–#5 from ROADMAP.md all observably met
- CHECK-04 can be marked complete in REQUIREMENTS.md traceability table
</success_criteria>

<output>
After completion, create `.planning/phases/02-duplicate-utc-day-detection-in-backfill-verifier/02-02-SUMMARY.md` following the standard summary template. Include: the PR number captured from `github-next-pr-number.sh`, confirmation that the wiring diff is +1/-0, and confirmation that all pre-existing tests in `./pkg/mimirtool/...` still pass.
</output>
