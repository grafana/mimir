server:
  http_listen_port: 80

auth:
  type: trust

blocks_storage:
  backend: s3

ruler_storage:
  backend: s3

alertmanager_storage:
  backend: s3

querier:
  active_query_tracker_dir: ./active-query-tracker

alertmanager:
  data_dir: data/

compactor:
  data_dir: ./data

ruler:
  rule_path: /rules

graphite:
  querier:
    schemas:
      backend: s3